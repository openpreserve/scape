all:
	@echo "Tika does not need to be compiled. Nothing to do. "
	@echo "To install, use: make install"

configure:

install:
	mkdir -p $(DESTDIR) 2>/dev/null
	cp tika-app-1.2.jar $(DESTDIR)
	cp executeJar.sh $(DESTDIR)
