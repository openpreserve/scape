This folder contains command line tools and corresponding files used for xa-toolwrapper.

extractFeatures and compare tool Version 1.0
============================================
The extractFeatures tool extracts histogram and profile data from passed image file and returns it in XML format to standard output.
The compare tool compares two XML files produced by extractFeatures tool and returns result in XML format to standard output.
Both tools are written in C++. Executables for Windows can be found in Release folder. Linux OS requires compilation of sources.
See INSTALL.txt in root folder.